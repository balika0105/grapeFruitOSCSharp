using Cosmos.System;
using System;
using Sys = Cosmos.System;

namespace grapeFruitOSCSharp
{
    public class KeycodeTest
    {
        public static void Main()
        {
            /*System.Console.Clear();
            System.Console.WriteLine("You are now in keyboard testing mode\nWatch the output to get the keycodes\nEsc to exit");
            KeyEvent key;
            char value = '\0';

            while (true)
            {
                key = KeyboardManager.ReadKey();
                if (key.Key != ConsoleKeyEx.Escape)
                {
                    System.Console.WriteLine($"Key: {key.Key}, Scan Code: {key.}");
                }
                else
                {
                    break;
                }
            }*/
            System.Console.WriteLine("Placeholder text");
        }
    }
}
